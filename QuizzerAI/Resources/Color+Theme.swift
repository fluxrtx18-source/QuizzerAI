// AppColor, AppearanceMode, and UIColor adaptive helpers live in ColorExtension.swift,
// which is registered in the Xcode project. This file is intentionally empty so that
// running `xcodegen generate` in the future will not produce duplicate symbols.
